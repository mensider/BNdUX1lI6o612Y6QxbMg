import networkx
import grid_graph

def swarm_algo(bot1_currentnode,bot1_target,bot2_currentnode,bot2_target,bot3_currentnode,bot3_target,bot4_currentnode,bot4_target):

    #Get next node from current node
    grid = grid_graph.grid_graph()

    bidirectional_shortest_path(grid, bot1_current_node, target)
    #Get coordinates for next node


    #Assign dx values accordingly

    return (bot1_nextnode,bot2_nextnode,bot3_nextnode,bot4_nextnode)
